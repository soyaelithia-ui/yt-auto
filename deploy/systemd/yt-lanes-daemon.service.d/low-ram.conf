# Low-RAM: alinear con docker-compose.small.yml (2g).
# Instalar: sudo cp -r deploy/systemd/yt-lanes-daemon.service.d /etc/systemd/system/
#           sudo systemctl daemon-reload && sudo systemctl restart yt-lanes-daemon
[Service]
MemoryMax=2G
CPUQuota=200%
